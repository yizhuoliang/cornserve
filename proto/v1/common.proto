syntax = "proto3";

package cornserve.common;

enum Status {
  STATUS_UNSPECIFIED = 0;
  STATUS_OK = 1;
  STATUS_ERROR = 2;
}

enum TaskType {
  UNKNOWN = 0;

  // LLMs and multimodal LLMs
  LLM = 1;
}
